<!DOCTYPE html>
<html>

<head>
    <title>Edit Book</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>

    <h1>✏️ Edit Book</h1>

    <form method="POST" class="book-form">
        <input name="title" value="<%= book.title %>" placeholder="Book Title" required>
        <input name="author" value="<%= book.author %>" placeholder="Author Name" required>
        <input name="rating" type="number" min="0" max="5" step="0.1" value="<%= book.rating %>"
            placeholder="Rating (0-5)">
        <input name="read_date" type="date" value="<%= book.read_date.toISOString().split('T')[0] %>">
        <input name="isbn" value="<%= book.isbn %>" placeholder="ISBN">
        <textarea name="notes" placeholder="Notes"><%= book.notes %></textarea>
        <button type="submit">Update Book</button>
    </form>

    <div style="text-align: center; margin-top: 20px;">
        <a href="/">⬅ Back to books</a>
    </div>

</body>

</html>